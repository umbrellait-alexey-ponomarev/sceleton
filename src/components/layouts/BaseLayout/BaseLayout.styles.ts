import { StyleSheet } from 'react-native';
import { deepBlack } from '../../../constants/UIColors';

export const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
    backgroundColor: deepBlack,
  },
  container: {
    width: '100%',
    height: '100%',
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: deepBlack,
  },
});
